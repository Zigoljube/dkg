---
type: Thing
---

A conformant concept that declares only the required `type` field — every
optional field (title, description, resource, tags, timestamp) is absent. A
consumer must accept it and degrade gracefully (SPEC §4.1/§9).
